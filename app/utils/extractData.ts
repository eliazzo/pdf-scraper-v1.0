// utils/extractData.ts

const extractData = (pdfData: any): any => {
    // Your data extraction logic goes here
    // For demonstration, we're just returning the text
    return { text: pdfData.text };
  };
  
  export default extractData;
  